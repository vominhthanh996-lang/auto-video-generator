name: Schedule Probe

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Show scheduler tick
        run: |
          date -u
          echo "Schedule probe fired."